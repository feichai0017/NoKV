profile_id: yanex_agent_benchmark_base_v1
version: 1
mode: warm_interface
api_surface: openai_agents_responses_schema_once

model:
  temperature: 1
  max_completion_tokens: 4096
  stream: false
  structured_output: true

run_policy:
  stateless: true
  repeats_per_arm_task: 10
  max_turns: 20
  max_tool_calls: 80
  tool_call_timeout_ms: 60000
  retry_policy: no_auto_success_retry
  clear_messages_after_run: true
  corpus_snapshot: fixed_per_benchmark_batch

base_system_message: |
  You are an experiment tracking analysis agent working over a Yanex experiment corpus.
  You must solve the requested task using only the interface tools exposed for the current benchmark arm.


base_developer_message: |
  Output exactly one JSON object matching the task's required answer shape.
  Do not include markdown outside the JSON object.


output_schema:
  type: object
  additionalProperties: true

telemetry_contract:
  record_types:
    - tool_call_start
    - benchmark_run
  capture_per_api_call:
    - request_id
    - response_id
    - previous_response_id
    - sent_tool_schema
    - sent_initial_instructions
    - model
    - started_at
    - completed_at
    - prompt_tokens
    - completion_tokens
    - total_tokens
    - reasoning_tokens
    - cached_prompt_tokens
    - accepted_prediction_tokens
    - rejected_prediction_tokens
  capture_per_tool_call:
    - call_id
    - tool_name
    - arguments
    - started_at
    - completed_at
    - status
    - bytes_read
    - result_token_estimate
    - error
    - timed_out
  derived_metrics:
    - interface_card_tokens
    - task_prompt_tokens
    - execution_cost_usd
    - all_in_cost_usd
    - wall_time_ms
    - task_success
    - invalid_sql_count
    - wrong_tool_count
    - tool_schema_repeated_count
    - tool_timeout_count
    - overread_bytes
